# 🐍 Python Practice for QA

> Путь перехода от ручного тестирования к автоматизации  
> Практика на Python — условия, циклы, списки, генерация SQL и мини-проекты

---

## 📚 Описание

Репозиторий содержит серию учебных и практических заданий, выполняемых в процессе освоения Python.  
Цель — наращивать навыки для автоматизации тестирования (AQA), переходя от базового синтаксиса к практике с `pytest` и API.

---
## 📁 Структура проекта

```
learning-basic-python/
├── control_if_while_for.py         # Контрольная по условиям и циклам
├── validator_login_password.py     # Валидатор логина и пароля
├── list_lesson.py                  # Задачи на списки
├── list_lesson_ch2.py              # Задачи на списки. День 2. задачи на max, min, remove, count, индексы, уникальность
├── list_lesson_ch3.py              # Задачи на списки. Закрепление пройденного материала
├── list_lesson_ch4.py              # Задачи на списки. Закрепление базовых методов, пройденного материала
├── list_lesson_ch5.py              # Задачи на закрепление списков.
├── string_lesson.py                # Контрольная работа по строкам и методам строк
├── control_strings_lists.py        # (в будущем) Строки и списки
├── control_functions_api.py        # (в будущем) Функции и API
├── test_functions.py              # Юнит-тесты на функции с использованием pytest
└── README.md                       # Описание проекта

```

## ✅ Пройденные темы

### 🔀 Условия и циклы

Файл: `control_if_while_for.py`

- Условия `if / elif / else`
- Циклы `while`, `for`, `break`, `continue`
- Проверка возраста, логина, повторный ввод
- Мини-игра “Угадай число”
- Таблица умножения

---

### 🔐 Валидация логина и пароля

Файл: `validator_login_password.py`

- Цикл до корректного ввода
- Проверка длины логина и пароля
- Проверка на совпадение логина и пароля
- `continue` при ошибках, `break` при успехе

---

### 🔢 Списки (`list`)

Файл: `list_tasks.py`  
6 задач по ключевым операциям:

- Суммирование чисел
- Удаление чётных
- Фильтрация слов по длине
- Ввод товаров с анализом корзины
- Отбор уникальных слов
- Перемешивание списка

---

## 🧩 Списки. День 2 — Закрепление
Файл: `list_lesson_ch2.py` 

- Ручной поиск max() и min()
- Удаление значений с remove() и while
- Подсчёт повторяющихся значений (аналог count())
- Поиск индекса вручную без .index()
- Сравнение срезов и элементов
- Перебор значений vs индексов (for in, for range)
- Уникальные значения с not in
- Сортировка со sort()
---

## 📘 Списки. День 3 — Закрепление + Углубление
Файл: `list_lesson_ch3.py` 

- Получение уникальных значений и сортировка по убыванию
- Поиск второго по величине элемента без использования sort()
- Перемещение всех 0 в конец списка, сохраняя порядок
- Деление чисел на чётные и нечётные
- Ручной поиск повторяющихся элементов

---

## 📘 Списки. День 4 — Полный практический разбор
Файл: `list_lesson_ch4.py`

 - Разминка на .append(), .insert(), .remove(), .pop(), .reverse()
 - Средние задачи: копирование списков, вставка в середину, подсчёт значений, ручная сортировка через pop/insert
 - Повторение: второй максимум, срезы, фильтрация, работа с копией списка
 - Методы: .index(), .copy(), .clear(), .sort(), .pop()
 - Самостоятельная попытка решения задачи вставки "X" через .insert() без счётчиков (зафиксировано как зона роста)

---

## 📘 Списки. День 5 — Финальное закрепление
Файл: `list_lesson_ch5.py`

 - Вставка "X" после каждого второго элемента (через while + insert)
 - Очистка списка с сохранением копии (copy() + clear())
 - Генерация квадратов через list comprehension
 - Фильтрация чётных чисел в новый список
 - Удаление повторяющихся значений во время прохода (срез [:])
 - Разворот копии списка, сравнение с оригиналом

---

## Строки. Контрольная работа
Файл: `string_lesson.py`

- ✅ **1. Маска email из строки**
  - Разбор строки по частям
  - Маскирование email с сохранением первой и последней буквы
- ✅ **2. Валидация полей user/email/id**
  - Проверка на наличие полей
  - Проверка, что id состоит из цифр
  - Маскирование email, вывод результата
- ✅ **3. Защита от кривого лога**
  - Проверка:
    - user не пустой
    - email содержит `@`, только латиница, длина > 2
    - id — только цифры
  - Сбор и вывод ошибок по каждому полю

🗂 Все задачи находятся в `string_lesson.py`  
🧠 Цель: закрепить работу со строками, условиями, `split`, `isdigit`, `isalpha`, `isascii`

## Словари. Первый день.
Файл: `dict_lesson.py`

- ✅ 1.Доступ и проверка ключей
  - Извлечение значений по ключу
  - Проверка наличия ключа с помощью in

- ✅ 2.Обновление и добавление
  - Обновление значения существующего ключа
  - Добавление новых пар ключ–значение

- ✅ 3.Перебор словаря
  - Использование .items() для получения ключей и значений
  - Вывод данных в читаемом формате

- ✅ 4. Удаление и безопасный доступ
  - Удаление ключей с del
  - Безопасное извлечение с помощью .get() и дефолтного значения

- ✅ 5. Фильтрация по значению
  - Отбор только тех ключей, где значение True

🗂 Все задачи находятся в dict_lesson.py

🧠 Цель: научиться работать со структурами dict, безопасно извлекать данные, перебирать словари, обновлять и фильтровать содержимое.

## Словари. Вложенные структуры и списки словарей
Файл: `dict_lesson2.py`

- ✅ 1. Доступ к вложенным полям
  - Получение значений из словаря внутри словаря (user["profile"]["email"])
  - Вывод нескольких значений в одной строке с помощью f-строк

- ✅ 2. Условная проверка вложенности
  - Проверка активности пользователя и наличия email
  - Безопасная логика: if user["active"] and "email" in user["profile"]

- ✅ 3. Обновление вложенного словаря
  - Изменение значений: city = Санкт-Петербург
  - Добавление нового ключа telegram во вложенную структуру

- ✅ 4. Перебор списка словарей
  - Фильтрация активных пользователей
  - Подсчёт количества пользователей и активных

- ✅ 5. Маскировка email в списке словарей
  - Обработка каждого email: оставлена 1-я и последняя буква логина, остальное заменено на *
  - Изменение значений непосредственно в словарях
  - 
🧠 Цель: освоить доступ, перебор и модификацию вложенных структур (dict внутри dict, списки словарей), уверенно работать с реальными JSON-подобными объектами.

---

---

## 🧪 Функции — Уровень 1 и 2 (База)

Файл: `def_lesson.py`

- `greet(name)` — возвращает приветствие по имени
- `add(a, b)` — сумма двух чисел
- `is_even(n)` — проверка на чётность
- `compare(a, b)` — сравнение чисел (больше/меньше/равно)
- `count_chars(text)` — длина строки без пробелов
- `square(n)` — квадрат числа
- `capitalize_first(text)` — делает первую букву заглавной
- `count_vowels(text)` — считает гласные в строке (русский алфавит)
- `check_password(password)` — определение сложности пароля
- `apply_discount(price, discount)` — цена с учётом скидки в %

🧠 Цель: закрепить синтаксис `def`, `return`, ветвления и простую логику

---

---


## 🧪 Функции — Прикладные задачи и логика

Файл: `def_lesson_ch2.py`

- `grade_to_score(grade)` — перевод "отлично/хорошо/..." в баллы (5–2)
- `is_passed(grade)` — проверка, сдано ли по оценке
- `apply_discount(price, discount)` — скидка в %
- `final_price(price, discount, vat_percent)` — сначала скидка, потом добавление НДС
- `count_correct(answers)` — считает ответы "OK"
- `check_test_result(answers, total_questions)` — возвращает "Сдано" / "Не сдано" по проценту ≥ 70

📌 Все функции далее покрыты автотестами в `test_functions.py`


### 📌 Задачи:

**`apply_discount(price, discount)`**
- Простая функция для расчёта скидки в процентах

**`final_price(price, discount, vat)`**
- Применение скидки и последующий расчёт НДС

**`check_test_result(answers, total_questions)`**
- Подсчёт процента правильных ответов
- Возврат "Сдано" / "Не сдано" по порогу в 70%

---
## 🧪 Функции — Углубление и парсинг строк

Файл: `def_lesson3.py`

- `parse_user_data(data_str)` — разбор строки `"ключ=значение"` в словарь  
  - Удаление пробелов  
  - Обработка дубликатов  
  - Пропуск мусора  

### ✅ Покрытие `pytest`:

Файл: `test_functions.py`

- `test_grade_to_score()` — проверка перевода оценок в баллы
- `test_is_passed()` — проверка прохождения по оценке
- `test_apply_discount()` — расчёт цены со скидкой
- `test_final_price()` — расчёт цены с НДС, `pytest.approx` для float
- `test_parse_user_data()` — проверка разбора строки в словарь
- `test_check_test_result()` — процент правильных, статус "Сдано"

---

🧠 В процессе: закрепление навыков автотестирования и логики  
🚀 Следующий шаг: покрытие API-функций, структура тест-проекта


## 📌 В планах

- Pytest и тест-проект
- Работа с API (`requests`)
- Структура автотестов

---

## ✍️ Автор

**Александр Миронов**  
Ручной тестировщик, 5+ лет опыта  
Переход в автоматизацию на Python  
[github.com/MironovAleksandr199304](https://github.com/MironovAleksandr199304)

---

